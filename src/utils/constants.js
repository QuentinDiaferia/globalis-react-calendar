import moment from 'moment'

export const weekDays = moment.weekdays()

export const navigation = {
    PREVIOUS: 'prev',
    NEXT: 'next',
}

export const views = {
    MONTH: 'month',
    WEEK: 'week',
}
